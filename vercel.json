{
    "builds": [{
        "src": "chat_app/asgi.py",
        "use": "@now/python",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "chat_app/asgi.py"
        }
    ]
}
