{
    "builds": [{
        "src": "chat_app/asgi.py",
        "use": "@now/python",
        "config": { "maxLambdaSize": "17mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "chat_app/asgi.py"
        }
    ]
}
